<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.modu.app.prj.user.mapper.UserMapper">
 
 
 	<select id="loginCheck" resultType="UserVO" parameterType="String">
		select *
		from memb
		where id= #{id}
	</select>
	
	<insert id="signUp" parameterType="UserVO">
		INSERT INTO memb (
											MEMB_UNI_NO
											, ID
											, PWD
											, NM
											, PH_NO
											, SNS
											, QUIT
											, GRD
											, REG_DT
											)
		VALUES						(
											#{'m' || memb_seq.val}
											,#{id}
											,#{pwd}
											,{nm}
											,{phNo}
											,'N'
											,'N'
											,sysdate
											)
	</insert>
	

 </mapper>
  
  