<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.modu.app.prj.board.mapper.BoardMapper">
 	
	<!-- Board 전체조회 --> 	
 	<select id="BoardList" resultType="BoardVO" parameterType="String"> 
	 SELECT * FROM brd
 	 WHERE patrici_memb_uni_no=#{particiMembUniNo}
	</select>
	
	<!-- Board 단건조회 -->
	<select id="BoardGet" resultType="BoardVO">
	SELECT * FROM brd WHERE brd_uni_no=#{brdUniNo}
	</select>
	
	<insert id="InsertBoard" parameterType="BoardVO">
	INSERT INTO brd (brd_uni_no, board_nm, pubc_yn, prj_uni_no, patrici_memb_uni_no, reg_dt)
	VALUES
	('b'||brd_seq.nextval,#{boardNm},'Y','punt10','ppmt10',sysdate)
	</insert>
 	
 </mapper>